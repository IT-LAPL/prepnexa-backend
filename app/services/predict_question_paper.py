"""Removed: use `PredictedPaperService` from `app.services.predicted_paper_service`.

This module was removed to eliminate duplication. Importing it will raise
an ImportError to force callers to migrate.
"""

raise ImportError(
    "predict_question_paper removed; use PredictedPaperService from app.services.predicted_paper_service"
)
